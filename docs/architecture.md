# Riadom Architecture

Riadom follows the [Bulletproof React](https://github.com/alan2207/bulletproof-react/) architecture: feature-based modules, unidirectional imports, and clean boundaries between shared code, features, and the app composition layer. The same principles apply to the NestJS backend.

For agent workflows, see [`.agents/skills/riadom-architecture/SKILL.md`](../.agents/skills/riadom-architecture/SKILL.md).

---

## Monorepo layout

```
riadom/
├── apps/
│   ├── web/                    # Vite + React frontend
│   └── api/                    # NestJS backend
├── packages/
│   ├── contracts/              # Zod schemas shared by web + api
│   ├── db/                     # Drizzle schema, migrations, DB client
│   ├── ui/                     # Design system (shadcn + Base UI)
│   ├── eslint-config/
│   ├── typescript-config/
│   └── prettier-config/
├── scripts/                    # Dev helpers (ngrok host tunnel)
├── docs/
│   └── architecture.md
├── docker-compose.yml          # Postgres (+ optional ngrok profile)
├── .env.example
└── AGENTS.md
```

### Package roles

| Package           | Purpose                                                      |
| ----------------- | ------------------------------------------------------------ |
| `@repo/contracts` | Single source of truth for API request/response shapes (Zod) |
| `@repo/db`        | Drizzle schema, migrations, and `createDb()` client factory  |
| `@repo/ui`        | Design system primitives only — Button, Card, Input, theme    |
| `apps/web`        | Product UI, feature modules, routing, app composition        |
| `apps/api`        | HTTP API, business logic, Better Auth, data access           |

---

## Frontend (`apps/web`)

### Folder structure

```
apps/web/src/
├── app/
│   ├── app.tsx                 # Composition root
│   ├── provider.tsx            # Theme, QueryClient, Router, Toaster
│   ├── router.tsx              # TanStack Router instance + query client
│   └── routes/                 # File-based routes (TanStack Router)
│       ├── __root.tsx
│       ├── index.tsx           # / → landing
│       ├── profile.tsx         # /profile
│       └── (auth)/
│           ├── _layout.tsx     # Auth shell layout
│           └── _layout/
│               ├── sign-in.tsx
│               └── sign-up.tsx
├── constants/
│   └── paths.ts                # Route path constants
├── features/
│   ├── auth/
│   │   ├── api/
│   │   │   └── auth-client.ts  # Better Auth React client
│   │   ├── components/
│   │   ├── constants/
│   │   ├── pages/
│   │   └── utils/
│   └── landing/
│       ├── components/
│       ├── constants/
│       └── pages/
│           └── landing-page.tsx
├── hooks/                      # Shared hooks (e.g. use-in-view)
├── lib/
│   ├── api-client.ts           # Axios instance (credentials, /api base)
│   ├── env.ts                  # Vite env helpers
│   └── query-client.ts         # TanStack Query client factory
├── routeTree.gen.ts            # Generated by @tanstack/router-plugin
├── types/                      # App types not in @repo/contracts
└── main.tsx                    # Entry point
```

There is no `apps/web/src/components/` directory yet — shared, non-feature UI will live there when needed.

### Current routes

| Path        | Feature  | Page component                          |
| ----------- | -------- | --------------------------------------- |
| `/`         | landing  | `features/landing/pages/landing-page`   |
| `/sign-in`  | auth     | `features/auth/pages/sign-in-page`      |
| `/sign-up`  | auth     | `features/auth/pages/sign-up-page`      |
| `/profile`  | auth     | `features/auth/pages/profile-page`      |

Routes are thin: they import a page from a feature and wire search-param validation or loaders. Route files live in `app/routes/`; page components live in `features/<name>/pages/`.

The Vite plugin reads routes from `src/app/routes/` and generates `routeTree.gen.ts`. Do not edit the generated file by hand.

### Feature template

Create subfolders only when a feature needs them:

```
features/<name>/
├── api/          # Fetchers, auth clients, TanStack Query hooks
├── components/
├── constants/
├── hooks/
├── pages/        # Route-facing page components
├── stores/       # Optional feature-local state
├── types/
└── utils/
```

### Import rules

Imports flow in one direction: **shared → features → app**.

1. **Features must not import from other features.** Compose different features in `app/routes/`.
2. **Shared layers** (`hooks/`, `lib/`, `constants/`, `types/`) must not import from `features/` or `app/`.
3. **`app/`** may import from features and all shared layers.
4. **No barrel files** — import directly from the source file (e.g. `@/features/landing/pages/landing-page`, not `@/features/landing`).

```typescript
// Good — route composes a feature page
import { SignInPage } from '@/features/auth/pages/sign-in-page';

// Bad — cross-feature import
import { AuthForm } from '@/features/auth/components/auth-form'; // from inside features/landing/

// Bad — shared layer importing a feature
import { LandingPage } from '@/features/landing/pages/landing-page'; // from hooks/use-something.ts
```

### Where does new frontend code go?

```
Is it a design system primitive (Button, Input, theme token)?
  → @repo/ui

Is it specific to one product area (map, auth, activities)?
  → features/<name>/

Is it shared across multiple features but not a design primitive?
  → apps/web/src/components/

Is it a shared hook?
  → apps/web/src/hooks/

Is it an API fetcher or TanStack Query hook?
  → features/<name>/api/

Is it a generic HTTP/utility client?
  → apps/web/src/lib/

Is it an API shape (request/response)?
  → @repo/contracts

Does it define a URL or wire features together (route, provider)?
  → apps/web/src/app/
```

### What stays in `@repo/ui`

Design system only. Current components: `button`, `card`, `input`, `logo`, `section`, `sonner`, `theme-provider`. Do not put feature-specific UI, business logic, or app layouts in `@repo/ui`.

---

## Backend (`apps/api`)

### Folder structure

```
apps/api/src/
├── auth/                       # Better Auth setup (not a NestJS feature module)
│   ├── create-auth.ts          # betterAuth() factory + Telegram plugin
│   ├── constants/
│   ├── types/
│   └── utils/                  # Username sync, OIDC mapping, hooks
├── config/
│   └── env.ts                  # Typed env (Zod + @nestjs/config)
├── health/                     # Reference NestJS module
│   ├── health.controller.ts    # HTTP layer only
│   ├── health.service.ts       # Business logic
│   └── health.module.ts
├── app.module.ts               # ConfigModule, AuthModule, HealthModule
└── main.ts                     # Global /api prefix, CORS
```

Auth is mounted by `@thallesp/nestjs-better-auth` via `AuthModule.forRootAsync` — there is no separate `auth.controller.ts`. Better Auth handles `/api/auth/*` internally.

The `common/` folder (filters, guards, interceptors, pipes) does not exist yet. Add cross-cutting NestJS code there when needed.

### Module template

For new domain modules (activities, friends, etc.):

```
<feature>/
├── <feature>.module.ts
├── <feature>.controller.ts     # HTTP only — thin, delegates to service
├── <feature>.service.ts        # Business logic
└── <feature>.repository.ts     # Data access via @repo/db
```

DTOs come from `@repo/contracts` via `nestjs-zod`:

```typescript
import { createZodDto } from 'nestjs-zod';
import { HealthResponseSchema } from '@repo/contracts/health';

class HealthResponseDto extends createZodDto(HealthResponseSchema) {}
```

### Backend principles

| Rule                           | Detail                                          |
| ------------------------------ | ----------------------------------------------- |
| Thin controllers               | Controllers handle HTTP; services hold logic    |
| No cross-module file imports   | Modules expose services via NestJS `exports`    |
| Contracts in `@repo/contracts` | Never duplicate API shapes in the api app       |
| Common for cross-cutting       | Filters, guards, interceptors live in `common/` |
| Unidirectional flow            | `common/` → feature modules → `app.module.ts`   |

---

## Auth

Auth runs on Better Auth with the Drizzle adapter and Telegram OIDC.

| Layer   | Location                              | Role                                      |
| ------- | ------------------------------------- | ----------------------------------------- |
| Server  | `apps/api/src/auth/create-auth.ts`    | `betterAuth()` config, DB hooks, plugins  |
| NestJS  | `AuthModule.forRootAsync` in app module | Mounts auth routes, disables default body parser |
| Client  | `apps/web/src/features/auth/api/auth-client.ts` | React client, session hooks, Telegram sign-in |
| Schema  | `packages/db/src/db/auth-schema.ts`   | user, session, account, verification tables |
| Errors  | `packages/contracts/src/auth-errors.ts` | Shared auth error codes for redirects   |

The web app proxies `/api` to the API in dev. Auth requests use cookie credentials (`withCredentials: true` / `credentials: 'include'`).

Telegram OIDC requires a public HTTPS callback in development — use ngrok (`pnpm ngrok:up`, set `NGROK_DOMAIN` in `.env`). See `.env.example` for BotFather redirect URL format.

---

## Database (`@repo/db`)

```
packages/db/
├── src/
│   ├── db/
│   │   ├── auth-schema.ts      # Better Auth tables (+ tgUsername on user)
│   │   └── schema.ts           # Re-exports auth-schema (extend for app tables)
│   └── index.ts                # createDb(), schema export
├── drizzle/                    # Generated SQL migrations
└── drizzle.config.ts
```

Usage in the API:

```typescript
import { createDb, schema } from '@repo/db';
import { drizzleAdapter } from '@better-auth/drizzle-adapter';

const db = createDb(env.DATABASE_URL);
// drizzleAdapter(db, { provider: 'pg', schema })
```

Local Postgres runs via `pnpm db:up` (`docker-compose.yml`). Default credentials: `postgresql://riadom:riadom@localhost:5432/riadom`.

Commands (from repo root):

```sh
pnpm exec turbo run db:generate --filter=@repo/db
pnpm exec turbo run db:migrate --filter=@repo/db
pnpm exec turbo run db:studio --filter=@repo/db
```

PostGIS is planned for geospatial activity data but not configured yet.

---

## Contracts (`@repo/contracts`)

One file (or folder) per domain with a subpath export in `package.json`:

```
packages/contracts/src/
├── health.ts
├── auth-errors.ts
└── index.ts
```

Both `apps/web` and `apps/api` import from the same schema. The web client validates search params and responses; the api validates requests/responses via DTOs.

---

## Libraries

### Installed

| Concern        | Library                                         | Where used                          |
| -------------- | ----------------------------------------------- | ----------------------------------- |
| Routing        | `@tanstack/react-router`                        | `apps/web/src/app/routes/`          |
| Server cache   | `@tanstack/react-query`                         | `apps/web/src/lib/query-client.ts`  |
| API client     | Axios                                           | `apps/web/src/lib/api-client.ts`    |
| Auth           | `better-auth`, `better-auth-telegram`           | api + web auth feature              |
| Backend config | `@nestjs/config`                                | `apps/api/src/config/env.ts`        |
| Database       | Drizzle ORM + postgres.js                       | `@repo/db`                          |
| Validation     | Zod                                             | `@repo/contracts`, env schemas      |

### Not yet added

| Concern   | Recommended library                             |
| --------- | ----------------------------------------------- |
| Forms     | `react-hook-form` + `@hookform/resolvers` + Zod |
| App state | `zustand`                                       |
| PostGIS   | Drizzle + PostGIS extension                     |

When adding TanStack Query data layers for new features, follow the [Bulletproof API layer pattern](https://github.com/alan2207/bulletproof-react/blob/master/docs/api-layer.md): fetcher function → `queryOptions` → hook, colocated in `features/<name>/api/`.

---

## Product constraints

All architectural work must respect the product rules in [`AGENTS.md`](../AGENTS.md):

1. Map-first discovery
2. No in-app chat
3. Contact info gated behind approved activities
4. Location masked until approval
5. Trust & safety is core, not an add-on

---

## References

- [Bulletproof React](https://github.com/alan2207/bulletproof-react/) — project structure, API layer, state management
- [Bulletproof project structure](https://github.com/alan2207/bulletproof-react/blob/master/docs/project-structure.md)
- [TanStack Router](https://tanstack.com/router) — file-based routing
- [Better Auth](https://www.better-auth.com/) — authentication
- [NestJS modules](https://docs.nestjs.com/modules)
